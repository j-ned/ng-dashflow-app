# Le backend (et toute sa config : DB, JWT, SMTP, S3/R2, OAuth) vit désormais dans le repo nest-dashflow-app.
# Ce repo Angular n'a pas besoin de variables d'environnement au build (proxy dev dans proxy.conf.json).
